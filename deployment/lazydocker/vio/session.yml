name: docker
root: ./
socket_name: mrs
attach: false
tmux_options: -f /etc/ctu-mrs/tmux.conf
startup_window: lzd81
windows:
  - lzd81:
      layout: 6102,174x53,0,0[174x38,0,0,0,174x14,0,39,5]
      panes:
        - export DOCKER_HOST=tcp://uav81:2375 && lazydocker
        - export UAV_NAME=uav81; ros2 run mrs_uav_status status.sh
  - compose:
      layout: tiled
      panes:
        - export DOCKER_HOST=tcp://uav81:2375 && cd uav81 && ./up.sh
  - workspace:
      layout: tiled
      panes:
        - export DOCKER_HOST=tcp://uav81:2375 && cd ./colcon_workspace_builder
  - ssh:
      layout: tiled
      panes:
        - ssh uav63
  - rviz:
      layout: tiled
      panes:
        - export DOCKER_HOST=tcp://uav81:2375 && ros2 run rviz2 rviz2 -d ./config.rviz
  # - zenoh_router:
  #     layout: tiled
  #     panes:
  #       - export export ZENOH_ROUTER_CONFIG_URI=./zenoh_router.json5 && ros2 run rmw_zenoh_cpp rmw_zenohd
